// import mssql from 'mssql'

// export class DatabaseHelper{
//  private pool : Promise<mssql.ConnectionPool>
//   addInputsToRequest(request:mssql.Request, data:{[x:string]:string|number} = {}){
//     const keys = Object.keys(data)  //This appends
//   }
//  static async exec (storedProcedure:string, data:{[x:string]:string|number} = {}){
//         //creating a request
//         const request:mssql.Request() = (await this.pool).request() //empty no inputs
//  }
// }